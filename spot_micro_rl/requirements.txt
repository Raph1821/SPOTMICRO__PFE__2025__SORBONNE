# Spot Micro RL Requirements

# Core dependencies
numpy>=1.19.0
scipy>=1.5.0
gym>=0.17.0
pybullet>=3.0.0

# Deep RL (Phase 3)
torch>=1.10.0

# Optional dependencies for visualization
matplotlib>=3.3.0

# ROS dependencies (installed via apt)
# sudo apt install ros-noetic-rospy
# sudo apt install ros-noetic-sensor-msgs
# sudo apt install ros-noetic-geometry-msgs
