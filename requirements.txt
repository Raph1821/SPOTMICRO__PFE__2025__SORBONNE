# Python dependencies for Spot Micro PyBullet Simulation
# Install with: pip3 install -r requirements.txt

# Physics simulation
pybullet>=3.2.5

# Numerical computing
numpy>=1.19.0

# Note: ROS packages (rospy, sensor_msgs, etc.) are installed via apt, not pip
# See install_dependencies.sh for complete installation instructions

